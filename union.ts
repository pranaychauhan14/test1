//Typescript 1.4 gives new feature called "union". This is for the combine one or more type.
//we can use pipe symbol(|) for combining types in typescript.

var n : number|string = 'Mohan';
n=121;
console.log(`The vlaue of n is ${n}`);